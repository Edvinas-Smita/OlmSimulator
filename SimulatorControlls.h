#pragma once

#include <windows.h>

#include "Olm parts.h"
#include "Grid.h"

BOOL initCameraControlls(HWND hwnd);
BOOL initOlmPosControlls(HWND hwnd);
BOOL initOlmPhaseControlls(HWND hwnd);
BOOL initWeaponControlls(HWND hwnd);